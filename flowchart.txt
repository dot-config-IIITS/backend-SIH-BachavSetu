IMPORTANT  :
    NAMESPACE IS '/client' 
    Use it for every 'emit'

1) check token exists 

if (token_exists) :
    // Implie that this isn't the first time user is opening the app
    // if token exists phone no also exists in the local storage
    emit -> 
        route = '/verify_token'
        data = {'token' : token, 'phone' : phone }
    Capture the output on '/verify_token_result'
        Possible outputs :   
            // Auth success cases 
                {'status':'details not filled'}
                    Redirect to fill details page..

                {'status':'details filled}
                    Redirect to .... (not yet planned)

            // Auth fail cases -> redirect to login page, enter phone no, recieve otp etc etc...
                {'status':'token_not_present'}

                {'status':'user_doesn\'t_exist'}


else :
    // Implies user is opening the app first time 
    Enter Phone no page
    emit -> 
        route = '/get_otp'
        data = {'phone':phone}

    Fill OTP page 
    emit - >
        rotue = '/verify_otp'
        data = {'phone':phone, 'otp':otp}
    Capture output on '/verify_otp_result'
        Possible outputs : 
            //Auth success cases -> Save the token in local storage or something
                {'status':'details_not_filled','token':token}
                    Redirect to fill details page..

                {'status':'details_filled','token':token}
                    Redirect to.... (not yet planned)

            //Auth fail cases
                {'status':'Wrong OTP'} 
                    Display wrong OTP..

                {'status':'No OTP is sent to this phone no.'}
                    //You will not encounter this case, unless you send the wrong phone no in the above emit
        